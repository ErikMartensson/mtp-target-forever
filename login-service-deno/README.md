# MTP Target Login Service (Deno)

TypeScript/Deno reimplementation of the MTP Target login service.

## Requirements
- Deno v2.6.0 or later

## Setup
1. Initialize database: \`deno task setup\`
2. Start services:
   - HTTP: \`deno task http\` (requires admin)
   - Login: \`deno task login\`
